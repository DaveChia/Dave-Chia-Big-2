//The function below resets the passing condition once a player plays a round.

function resetCurrentPlayerPass(){
		
		controlPassDetails.passCount = 0;
		controlPassDetails.originalControlPlayer = controlPlayer;
		
	}
